import 'package:flutter/material.dart';
//TODO

class ConfirmationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Подтверждение выбора")),
      body: Center(
        child: Text("Здесь будет итог выбора"),
      ),
    );
  }
}
